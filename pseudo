// CRUD
//
// a system that follows CRUD principals in order
// to keep track of players and their messages
// with perminance implemented
//
// Create
// 	Parameters: uniq_id, tag, points, inventory
// 	Returns: success or indication of failure
//
// 	create an entry in a csv file with parameters
// 	as values, respectively ordered
//
// Read
//	Parameters: tag
//	Returns: object[] (holding csv row entries)
//		 holding values for each value
//		 CSV file, getter functions for safety
//	
//	takes a tag and looks throgh a csv file in
//	search of tag name, returns an object with
//	processed values
//
// Update
// 	Parameters: uniq_id, change_object
// 	Returns: new object holding row
//
//	Process change_object's possible values
//	with a predefined entry-point to iterate
//	over all possible values and add or subtract
//	them from any existing values for the
//	object retrieved from the existing row
//
// Delete
// 	Parameters: uniq_id || tag
// 	Returns: object holding deleted row's values
// 		on success. empty object on failure.
//
//
//
// NOTE: uniq_id corresponds to row-number or index
// 	of a csv file
// NOTE: change_object is an object that holds optional
// 	values, tag, points, inventory and has functions
// 	to operate on arbitrary, existing values for each
// 	of the objects it holds--these functions are only
// 	defined once and do not need to be rewritten upon
// 	instantiation
